/*
  export function registerSocketEvents(socket: Socket, services: Services) {
    socket.on("join-matchmaking", (data) => services.matchmaking.handleJoin(socket, data));
    socket.on("player-move", (data) => services.battle.handleMove(socket, data));
    socket.on("player-attack", (data) => services.battle.handleAttack(socket, data));
  // etc.
}
*/