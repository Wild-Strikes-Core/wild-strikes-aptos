{
  "name": "wildverse-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "apps/web/*",
    "apps/web/app/*"
  ],
  "devDependencies": {
    "turbo": "^2.8.13"
  },
  "packageManager": "pnpm@8.15.4",
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
